# Dev profile: JWT auth is bypassed by DevSecurityConfig + DevAuthFilter.
# Exclude the OAuth2 resource server auto-configuration so Spring does not
# attempt to reach the auth service issuer URI at startup.
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration
