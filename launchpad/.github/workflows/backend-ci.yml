name: launchpad-backend-ci

# Run CI for LaunchPad backend changes on PRs and on pushes to main
on:
  push:
    branches: [main]
    paths:
      - backend/src/**
      - backend/pom.xml
      - backend/mvnw
      - backend/mvnw.cmd
      - backend/.mvn/**
      - backend/Dockerfile
      - compose.yaml
      - .github/workflows/backend-ci.yml
  pull_request:
    branches: [main]
    paths:
      - backend/src/**
      - backend/pom.xml
      - backend/mvnw
      - backend/mvnw.cmd
      - backend/.mvn/**
      - backend/Dockerfile
      - compose.yaml
      - .github/workflows/backend-ci.yml

jobs:
  # Validate the service against a real Postgres dependency
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: launchpad_db
          POSTGRES_USER: launchpad_user
          POSTGRES_PASSWORD: launchpad_pass
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U launchpad_user -d launchpad_db"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      # Point Spring to the service-container database in GitHub Actions
      SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/launchpad_db
      SPRING_DATASOURCE_USERNAME: launchpad_user
      SPRING_DATASOURCE_PASSWORD: launchpad_pass
      # Auth service is not started in CI test job, keep issuer explicit
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: http://localhost:8081

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Run unit tests
        working-directory: backend
        run: ./mvnw test

      - name: Upload surefire reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: launchpad-surefire-reports
          path: backend/target/surefire-reports/**

  # Build and publish image only for pushes to main after tests pass
  build-docker:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          # Build from backend/ directory and publish to GitHub Container Registry with both "latest" and commit SHA tags
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/hatchloom-launchpad:latest
            ghcr.io/${{ github.repository_owner }}/hatchloom-launchpad:${{ github.sha }}
